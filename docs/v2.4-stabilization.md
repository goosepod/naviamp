# v2.4.0 stabilization

> Historical September 6 branch record. These two stabilization commits were not included in
> the published v2.4.0 tag. They were recovered into main on September 9; see
> [the reconciliation record](v2.4-stabilization-reconciliation.md). The dated checks below
> describe that earlier candidate, not the released artifact or fresh hardware acceptance.

Started 2026-09-06 from `c6b3c648` on `feature/library-discovery-playlists`.
Stabilization branch: `release/v2.4.0`.
Target version: v2.4.0, build 50. Feature development is frozen for this release preparation.
This file supersedes historical acceptance checkboxes in the branch plan where evidence is cited.

## Gates

- [x] Review shared ownership: branch product changes live in Core and provider common code.
- [x] Review migration baseline: main/v2.3.1 ends at migration 23; branch changes are consolidated
  into migration 24. Do not add a second branch-development migration.
- [x] Reconcile recorded Windows browsing, search, Home, playlist, and playback smoke tests:
  [Windows audit](windows-usability-audit.md) and [player workspace](player-workspace.md).
- [x] Reconcile recorded Pixel library, playlist reconciliation, and OpenSubsonic live checks:
  [branch plan](library-discovery-playlists-plan.md) and [API audit](opensubsonic-api-audit.md).
- [x] Resolve reproducible Windows native EQ test failure and verify actual audio processing.
  All eight BASS/JNI integration tests pass, including measured +6 dB at 31 Hz, 62 Hz, and 16 kHz,
  replacement without accumulating effects, and return to flat after disabling EQ.
- [x] Complete local architecture, migration, shared/provider, Android, Desktop, and coverage gates.
- [x] Validate the changed JNI equalizer on a physical Android device; Pixel 10a/API 37 passed
  all four native-boundary instrumentation tests, including measured EQ gain and reset behavior.
- [x] Compile the shared equalizer/domain changes for Android, JVM, and iOS arm64
  (`build/stabilization-common-final.log`). Native iOS host validation remains separate below.
- [ ] Obtain green macOS/iOS native build and simulator checks for the final release candidate.
  Deferred until Tuesday, September 8: the user has no macOS machine available before then.
- [x] Build Windows standalone release ZIP and verify bundled native dependencies.
- [ ] Build/validate Windows MSI/EXE installers on the release runner.
- [x] Record Windows album catalog G jump, search, and restart acceptance.
- [ ] Record hands-on large Appears On catalog. Synthetic large-discography tests already exist;
  the live sample below does not establish large-catalog paging acceptance.
- [ ] Verify a v2.3.1-to-v2.4.0 packaged upgrade with preserved settings and library data.
- [x] Prepare VERSION, VERSION_CODE, changelog, and user-facing release notes.
- [x] Add Split/Full settings sync round-trip/default coverage; verify all 539 English/Spanish
  resource keys match with no duplicate Spanish keys.
- [ ] Final release review, tag/publish, and required GitHub Announcements Discussion.

## Initial verification

The first local pass validated 1,677 shared/provider JVM tests, architecture, migration checks,
Android debug unit tests, and Android debug native packaging. Desktop platform tests passed 39/40.
The generated WAV decode/EQ test failed again in isolation. Windows DX8 PARAMEQ rejects the 31 Hz
and 62 Hz bands and restricts the upper center frequency based on sample rate. See the
[native API documentation](https://www.un4seen.com/doc/bass/BASS_DX8_PARAMEQ.html).

CMake is installed at the Android SDK's `cmake/3.22.1/bin/cmake.exe`; this shell needs `CMAKE_EXE`
set explicitly. The initial PATH failure is environmental, separate from the native test failure.

Local evidence: `build/release-readiness-check.log`, `build/release-readiness-retry.log`, and
`build/release-readiness-native-repro.log`. Coverage and Android release tests were not completed
by the interrupted full gate. GitHub does not yet contain the reviewed feature commit, so its CI
cannot establish readiness. Native iOS checks require a macOS runner.

## Platform diff accountability

- `platforms/android/src/main/kotlin/app/naviamp/android/playback/AndroidBassJni.kt`: reads the native
  stream frequency and marshals the shared equalizer plan into the JNI parameter array.
- `platforms/desktop/src/desktopMain/kotlin/app/naviamp/desktop/playback/bass/DesktopBassJniBinding.kt`:
  loads Windows DLLs through `System.load`, reads native stream frequency, and marshals the shared plan.
- `platforms/ios/src/iosMain/kotlin/app/naviamp/ios/playback/IosBassPlayback.kt`: translates the shared
  plan into `BASS_DX8_PARAMEQ` cinterop fields and manages native effect handles; the duplicate
  frequency list and gain policy were removed.
- `native/bass-jni/src/naviamp_bass_jni.cpp`: resolves Windows BASS FX symbols and translates shared
  parameter triples into BFX/DX8 structs, managing the native effect handle lifetime. Partial failures
  now remove all effects created by that call.

Build metadata additionally requires `bass_fx.dll` in Windows native inputs and packaged resources.
Vendor copyright/provenance files are retained alongside the DLL; input validation treats only
native-library extensions as libraries.

Shared domain tests and Android/JVM/iOS arm64 domain compilation passed before host wiring
(`build/stabilization-common-final.log`). The iOS platform adapter itself cannot compile on this
Windows host: Gradle skips that task. Its final compile/native validation is an explicit Tuesday gate.

## Tuesday macOS checks

Run the iOS job's commands from `.github/workflows/verify.yml` on the final candidate, including
`iosSimulatorArm64Test`, the device and simulator Kotlin host compilations, the production Xcode
simulator build, and the Keychain boundary test. Run Desktop native tests and macOS packaging there
as well. Record the commit, Xcode/SDK versions, test counts, and package results before tagging.

## Local gate results (2026-09-06)

`build/stabilization-full-gate.log`: BUILD SUCCESSFUL, 7m 27s, 594 tasks. The run included the
architecture guard, SQLDelight migration validation, Android native package verification, and
`:koverVerify` with the existing 60% aggregate line-coverage floor.

| Suite | Tests | Failures / errors / skips |
| --- | ---: | --- |
| Shared/provider JVM | 1,681 | 0 / 0 / 0 |
| Desktop | 43 | 0 / 0 / 0 |
| Android debug unit | 1,455 | 0 / 0 / 0 |
| Android release unit | 1,455 | 0 / 0 / 0 |

Counts include shared behavior executed on more than one target; they are not unique test cases.
The migration regression now checks preservation of the saved server, library selection, and an
existing album's original release year, as well as all three new tables. This is a database upgrade
regression, not a claim that the released installer upgrade has been exercised.

No Android device was connected during the initial local gate. The subsequent physical-device
pass below validates the changed JNI build; earlier Pixel browsing results remain historical evidence.
Version validation and the comparison against `origin/main` pass for v2.4.0/build 50.
Source whitespace checks pass. The unchanged upstream `bass_fx.txt` is excluded from that check:
its section separators trigger Git's conflict-marker heuristic and it retains upstream trailing
spaces. Its SHA-256 matches the readme extracted from the official archive.

## Windows release candidate acceptance

`build/stabilization-windows-package.log`: BUILD SUCCESSFUL, 1m 19s. Standalone artifact:
`apps/desktop/build/compose/distributions/Naviamp-v2.4.0-windows-x64-release.zip` (94,508,443 bytes).
The packaged `bass_fx.dll` SHA-256 matches the vendored DLL; the Java runtime includes
`jdk.accessibility`. This does not claim Windows screen-reader acceptance or installer validation.

The actual packaged executable under `apps/desktop/build/compose/binaries/main-release/app/Naviamp/`
was launched at 306 × 593 window pixels. It restored the saved Navidrome connection, appearance,
Home sections, and paused Lane 8 / Nuclear Lethargy playback state. Albums opened successfully;
the G rail jump placed Pharrell Williams' `G I R L` first, followed by `G-SIDES` and `G.O.A.T.`.
Artwork loaded after the jump. Searching `G I R L` narrowed the list to the matching album; Clear
restored the catalog. No timing or network-request-count claim is inferred from these observations.

After closing and relaunching the packaged app, Albums opened again and the G jump reproduced the
same ordering. This is warm/restart acceptance using the existing development profile, not a
fresh-index or v2.3.1 installer-upgrade test.

Artist search found Snoop Dogg and opened a discography containing eight primary releases with
Albums, Compilations, and Other Releases sections. That sample did not expose a large Appears On
section, so the real-server paging gate remains open. The app was returned to Home with playback
paused. No playlists, favorites, stations, downloads, or credentials were edited.

## Physical Android native acceptance (2026-09-06)

Pixel 10a, Android API 37: `AndroidNativeBoundaryInstrumentedTest` completed with **OK (4 tests)**.
The new EQ regression measures settled generated-tone output at 31 Hz, 62 Hz, and 16 kHz, checks
approximately +6 dB gain, repeats configuration to reject stacked effects, and disables EQ to
verify a return to the original signal. The same run checks native codec loading, audio decode and
seek, Core's 5.1-to-stereo matrix through BASSmix, and Android Keystore encryption/tamper rejection.
These are native processing tests; they do not claim speaker/headphone listening acceptance.

The installed `.v2test` app uses a different signing certificate from this host's debug key. To
preserve all existing app data, a temporary Gradle init script changed only the debug application-ID
suffix to `.stabilization`. Both isolated packages were absent before installation, and both were
removed after testing. The regular, review, and v2test Naviamp installations were retained; the
existing v2test APK path was unchanged. Tests used generated local audio and no server account.

Evidence: `build/stabilization-phone-isolated-build.log` (successful APK/instrumentation build),
`build/stabilization-phone-native-tests.log` (test results), and
`build/stabilization-phone.init.gradle` (temporary package isolation).
